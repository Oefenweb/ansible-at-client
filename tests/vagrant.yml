# test file for at-client
---
- hosts: all
  remote_user: vagrant
  become: true
  pre_tasks:
    - include: pre.yml
  roles:
    - ../../
  vars:
    at_client_key_map:
      - src: "{{ playbook_dir }}/../files/id_rsa"
