# vars file for at-client
---
at_client_key_map:
  - src: "{{ at_client_private_key_file }}"

at_client_ssh_directory: "{{ playbook_dir }}/../files"
at_client_private_key_file: "{{ at_client_ssh_directory }}/id_rsa"
