# test file for at-client
---
- hosts: localhost
  connection: local
  become: true
  pre_tasks:
    - include: pre.yml
  roles:
    - ../../
  vars:
    at_client_key_map:
      - src: "{{ playbook_dir }}/../files/id_rsa"
